# R8 runs on release builds because the Flutter Gradle Plugin enables it.
# No app-specific keep rules are needed right now: the plugins in use ship
# their own consumer rules. Add rules here if a release build starts failing
# on missing classes, or if something breaks at runtime that works in debug.
